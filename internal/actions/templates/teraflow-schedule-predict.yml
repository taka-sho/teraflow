# teraflow-schedule-predict.yml
# TF-018: Schedule Predictワークフロー
# このファイルは teraflow setup actions で生成されます
name: teraflow-schedule-predict
on:
  schedule:
    - cron: '0 0 * * 1'

jobs:
  schedule-predict:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO: implement schedule predict"
