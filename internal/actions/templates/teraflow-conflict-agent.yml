# teraflow-conflict-agent.yml
# TF-012: Conflict Agentワークフロー
# このファイルは teraflow setup actions で生成されます
name: teraflow-conflict-agent
on:
  issues:
    types: [labeled]

jobs:
  conflict-agent:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO: implement conflict-agent"
