# teraflow-incident-agent.yml
# TF-016: Incident Agentワークフロー
# このファイルは teraflow setup actions で生成されます
name: teraflow-incident-agent
on:
  issues:
    types: [labeled]

jobs:
  incident-agent:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO: implement incident-agent"
