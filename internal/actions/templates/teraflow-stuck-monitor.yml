# teraflow-stuck-monitor.yml
# TF-013: Stuck Monitorワークフロー
# このファイルは teraflow setup actions で生成されます
name: teraflow-stuck-monitor
on:
  schedule:
    - cron: '0 0 * * 1-5'

jobs:
  stuck-monitor:
    runs-on: ubuntu-latest
    steps:
      - run: echo "TODO: implement stuck-monitor"
