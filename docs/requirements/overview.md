---
node_id: "req:overview"
title: "要件概要（互換ノード）"
depends_on:
  - "req:teraflow-overview"
tags: ["requirements", "overview"]
status: approved
---

# 要件概要（互換ノード）

この文書は、既存ドキュメントが参照する `req:overview` ノードを提供するための互換ノードである。
実体要件は [teraflow 要件概要](./teraflow-overview.md) を正とし、本ノードはその参照窓口として扱う。

## 位置づけ

- 参照互換性を維持しつつ、要件の中心ノードを `req:teraflow-overview` に統一する
- 既存 depends_on 参照の段階的移行を可能にする
